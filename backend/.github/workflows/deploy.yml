name: CD - Backend Deploy

on:
  push:
    branches:
      - develop

env:
  REGISTRY: registry.digitalocean.com/graduation-travel
  API_IMAGE_NAME: backend-api
  AI_IMAGE_NAME: ai-service

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install API dependencies
        working-directory: ./api-service
        run: npm install

      - name: Build API app
        working-directory: ./api-service
        run: npm run build

      - name: Login DO Registry
        uses: docker/login-action@v3
        with:
          registry: registry.digitalocean.com
          username: ${{ secrets.DO_REGISTRY_TOKEN }}
          password: ${{ secrets.DO_REGISTRY_TOKEN }}

      - name: Build API Docker Image
        working-directory: ./api-service
        run: |
          docker build -t $REGISTRY/$API_IMAGE_NAME:latest .

      - name: Push API Image
        run: |
          docker push $REGISTRY/$API_IMAGE_NAME:latest

      - name: Build AI Docker Image
        working-directory: ./ai-service
        run: |
          docker build -t $REGISTRY/$AI_IMAGE_NAME:latest .

      - name: Push AI Image
        run: |
          docker push $REGISTRY/$AI_IMAGE_NAME:latest

      - name: Deploy to Droplet
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: root
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd /root/app
            echo "${{ secrets.DO_REGISTRY_TOKEN }}" | docker login registry.digitalocean.com -u "${{ secrets.DO_REGISTRY_TOKEN }}" --password-stdin
            if docker compose version >/dev/null 2>&1; then
              DC="docker compose"
            else
              DC="docker-compose"
            fi
            $DC pull backend-api ai-service
            $DC rm -f backend-api ai-service
            $DC up -d --no-deps backend-api ai-service
            $DC ps
